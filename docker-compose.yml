services:
  mysql-bootcamp:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_bootcamp
    ports:
      - "3308:3306"
    volumes:
      - mysql_bootcamp_data:/var/lib/mysql

volumes:
  mysql_bootcamp_data: